# app/models/layout_parser.py
from pathlib import Path
from typing import Dict, Any

def parse_layout(image_path: Path) -> Dict[str, Any]:
    """
    Placeholder for page-layout analysis.
    Returns an empty structure to keep the interface stable.
    """
    return {"blocks": [], "lines": []}
